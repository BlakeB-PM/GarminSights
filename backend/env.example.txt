# Garmin Connect Credentials
GARMIN_EMAIL=your@email.com
GARMIN_PASSWORD=your_password

# Database
DATABASE_PATH=./fitness.db

# Garth Session Tokens
GARTH_TOKENS_PATH=./.garth_tokens

# AI Coach (Anthropic)
ANTHROPIC_API_KEY=sk-ant-...
# Optional: override the AI model (defaults to claude-sonnet-4-6)
# COACH_MODEL=claude-sonnet-4-6

# CORS - comma-separated list of allowed frontend origins (web deployment)
# Example: CORS_ORIGINS=https://app.yourdomain.com,https://www.yourdomain.com
# Defaults to http://localhost:5173,http://localhost:3000
# CORS_ORIGINS=https://app.yourdomain.com
